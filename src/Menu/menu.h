#ifndef POS_SEMESTRALKA_MENU_H
#define POS_SEMESTRALKA_MENU_H

#include "../Canvas/canvas.h"

void menuClient(char* host, char* port);

void menuPause(CANVAS* canvas);

#endif //POS_SEMESTRALKA_MENU_H
